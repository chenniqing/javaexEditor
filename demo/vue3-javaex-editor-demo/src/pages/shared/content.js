export const savedContentKey = "javaex-editor-demo-saved-content";

export function getDefaultEditorHtml() {
  return "<h2>javaexEditor 默认示例</h2><p>这个页面展示除 AI 以外的全部内置工具栏能力。</p><pre><code class=\"language-js\">const editor = createEditor(editorHost.value, options);</code></pre>";
}
